{
  "name": "playwright-1.51.0-bugs",
  "scripts": {
    "test:negative-duration:chromium": "playwright test --project chromium",
    "test:negative-duration:chromium:timeout": "playwright test --project chromium --global-timeout 2100000",
    "test:negative-duration:firefox": "playwright test --project firefox",
    "test:negative-duration:firefox:timeout": "playwright test --project firefox --global-timeout 2100000",
    "test:negative-duration:webkit": "playwright test --project webkit",
    "test:negative-duration:webkit:timeout": "playwright test --project webkit --global-timeout 2100000"
  },
  "dependencies": {
    "@playwright/test": "1.51.0",
    "@types/node": "^22",
    "playwright": "1.51.0",
    "playwright-core": "1.51.0"
  }
}
